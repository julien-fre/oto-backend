"""Clients du service FOD — les données publiques FR servies hors de cette box (ADR 0028).

Package sans surface propre : `http` porte le transport partagé, chaque autre module
un domaine de données (entreprises, foncier, urbanisme, santé, droit…).
"""
