"""Le connecteur côté PLATEFORME : son cran d'activation, sa sélection, ses identités,
sa fiche how-to, sa sonde de connexion (ADR 0010/0011/0019/0024).

Package sans surface propre. À ne pas confondre avec ses deux voisins : `providers/`
porte le REGISTRE (la déclaration de chaque connecteur), `tools/` porte les outils
exposés à l'agent. `docs/` (ici) est de la DONNÉE — un markdown par connecteur, lu
par `docs_reader`.
"""
