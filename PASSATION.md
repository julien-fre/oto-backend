**15:35 UTC — #872 reprise** (titre qualifié « connecteur `pennylane`, API publique, pas la GED » ; quatre pièces A→D ; chemin `lettering` tranché sur l'OpenAPI ; module à 486 lignes, découpage inscrit ; `scopes` de `pennylane_ref(kind="company")` vérifié par appel réel). Ce qui ne s'est pas vérifié : le précédent de découpage date d'hier (3e424cd1), pas de la semaine dernière ; la règle des 500 lignes n'est outillée nulle part (modules à 1653, 1577 ; `pennylaneged.py` à 501) ; la « contradiction » de scopes n'en est pas une (éclatement du scope `ledger` en janvier 2026, clé réelle porte les deux). Fait neuf : l'ordre des lignes rendues par `POST /ledger_entries` n'est pas garanti. Porteur pennylane prévenue, elle inscrit son nom dans l'issue.
**15:33 UTC — tronc backend +532a3cb8 « écrire sans préciser, c'est écrire chez SOI »** (procédure : régime privé par défaut étendu à l'écriture sans portée). **Attribué à dev 1 par le trailer `Claude-Session`** (identique à 684055b8), pas par l'auteur git. Changement de comportement servi, à dire avant le prochain tag. Toujours pas #870.
**Registre, ajouté : « signup login »** — parcours signup/login du dashboard, pour Alexis directement ; fait aujourd'hui : email+mot de passe au signup, QR TOTP réparé, compte client débloqué, réinitialisation 2FA admin (dans v1.192.0) ; ouvert : oto-private#86 (reconstruire l'interface de connexion). N'attend rien. Sans chevauchement avec oto#670 ni #161.
